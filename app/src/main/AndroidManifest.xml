<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iitevent.project.iit.activities" >
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/login_page" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EventListViewActivity"
            android:label="@string/events_page" >

        </activity>

        <activity
            android:name=".EventCreatedActivity"
            android:label="@string/event_created" >

        </activity>
        <activity
            android:name=".RegisterUserActivity"

            android:label="@string/user_reg_page" >

        </activity>
        <activity
            android:name=".UserMenuActivity"
            android:label="Menu" >
        </activity>
        <activity
            android:name=".RegisterEventActivity"
            android:label="Event Register" >

        </activity>
        <activity android:name=".EventDetailActivity"
            android:label="Event Detail">

        </activity>
        <activity android:name=".EventConfirmActivity"
            android:label="Registered!!">

        </activity>
    </application>

</manifest>
